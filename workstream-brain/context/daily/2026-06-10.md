# 2026-06-10 — daily context

## Meetings
- **Skylark Metrics Sync** (attendees: Insights PM, Dana, Marco, Priya) — Closed the active-user definition debate. Marco confirmed `content_edited` fires at edit-commit, before the save round-trip, which settled the "intent vs success" question in favor of `content_edited`. Priya signed off on rebuilding the core dashboard on it. **Decision:** active-user signal = `content_edited`. **Owed:** Insights PM to write the rationale into the methodology doc (done same day).
- **Insights × Growth standup** (attendees: Insights PM, Sam) — Sam pushed for an "active ever-before" retention denominator; Dana prefers "active last week." Unresolved. **Owed:** decision before retention dashboard ships.

## Email
- **"API edits missing source tag"** (Dana → Insights PM) — Dana confirmed public-API edits don't populate `properties.source`, so the interactive/automated slice can't assume missing = interactive. Agreed to treat missing as its own bucket.

## Chat
- **#telemetry-onboarding** — Marco noted the `doc_shared` mobile under-firing has no fix ETA yet; team agreed to restrict share-based funnel numbers to web/desktop in the interim.

## Open questions / follow-ups raised today
- Retention denominator: accounts active last week vs ever-before? (Dana vs Sam)
- `doc_shared` mobile fix timeline (Marco)
