# Skylark × Insights — Weekly Update (week of 2026-06-08)

> **Fictional example** of a published update, as `/weekly-update` would render it.

## Headlines
- Active-user definition **settled** on `content_edited`; core usage dashboard rebuilt and live on it.
- Retention dashboard **blocked** on a denominator decision.
- Adoption funnel **blocked** on `doc_shared` under-firing on mobile.

## By workstream

### Active-user definition & core dashboard
Closed the long-running debate. Anchor on `content_edited` (the edit-commit / intent moment); rejected `app_opened` (fires on tab focus — inflates 3–4×) and `edit_saved` (drops failed/in-flight saves). Marco confirmed the emit timing; rationale is now canon in the methodology doc. Core dashboard rebuilt on the canonical signal and validated against the internal/external split.

### Retention dashboard
Built and reviewed. Blocked on a definition call: is the weekly-return denominator "accounts active last week" (Dana) or "accounts active ever before" (Sam)? Needs a decision before ship.

### Adoption funnel
Onboarded → first-doc working. First-share step understated ~15–20% on mobile because `doc_shared` under-fires; restricted to web/desktop as an interim measure.

## Decisions made this week
- Active-user signal = `content_edited` — Insights PM + Marco — 2026-06-10.

## Open items / blockers
- Retention denominator definition — Dana / Sam — before retention ship.
- `doc_shared` mobile under-firing fix — Marco — no ETA.
- `properties.source` missing on API edits — Dana — affects interactive/automated slice only.

## Next week
- Drive the retention denominator decision to close.
- Confirm the `doc_shared` fix timeline with eng; decide whether to ship the funnel with a web/desktop caveat.
