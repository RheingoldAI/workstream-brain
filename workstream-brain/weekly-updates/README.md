# weekly-updates/

Layer 3 — permanent archive of every published cross-team status update, one file per week (`update-<YYYY-MM-DD>.*`). Written by `/weekly-update` on every publish.

This is the auditable record of what was communicated, when. Keep it; it's the institutional memory of the workstream's narrative arc.

> **Fictional example** below. When adapting, delete the example update(s) — keep this README.
