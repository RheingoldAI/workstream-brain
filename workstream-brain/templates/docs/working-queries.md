# Working queries — <workstream name>

> Proven queries/artifacts with caveats inline, organized by metric. Keep a runnable copy of each in `queries/`.

## <metric name>
<One line on what it measures and any caveat.>

```
<runnable query>
```

> ⚠ <caveat — the thing that makes this subtly wrong if ignored>
